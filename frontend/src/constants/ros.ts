/** Default port of the clover2_http backend. */
export const HTTP_PORT = 8080;

/** Cap on the rolling message buffer of the topic stream. */
export const STREAM_BUFFER_CAP = 100;

/** Cap on messages rendered by TopicDetail (newest first). */
export const RENDER_MESSAGE_CAP = 20;
