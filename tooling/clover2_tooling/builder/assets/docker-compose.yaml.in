services:
  clover2-docs:
    image: ${REGISTRY}clover2-docs:${CLOVER2_GIT_HASH}
    restart: unless-stopped
    ports:
    - "9000:80"

  clover2-frontend:
    image: ${REGISTRY}clover2-frontend:${CLOVER2_GIT_HASH}
    restart: unless-stopped
    ports:
    - "80:80"
