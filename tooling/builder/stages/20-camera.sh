# libcamera_install() {
#     log_info "Install libcamera package"
#     sudo apt-get update
#     sudo apt install -f /home/$USER/.clover2_backup/deb/libcamera-*-arm64.deb
# }

# libcamera_install

# sudo apt-get autoclean
# sudo apt-get clean

log_info "Install libcamera package. Skip..."
